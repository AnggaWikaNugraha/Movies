import React from 'react'

const Header = ({ type }) => {
    switch (type) {

        default:
            return (
                <div>
                    header
                </div>
            )
    }

}

export default Header
